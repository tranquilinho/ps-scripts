#!/bin/bash

readonly service_cfg=${0%/scripts*}/etc/service.cfg

. ${service_cfg} && . ${scripts_base}/common

readonly pkg_name=openssl
readonly pkg_version=1.0.1j
readonly url_base=http://www.openssl.org/source/

build_preconditions

./config --prefix=${prefix} --shared | logalize $(configure_log)

standard_install

# !!!! it seems to not to finishthe first make... running a second time succeeds (???)
